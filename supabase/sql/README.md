# SQL helpers (Supabase)

- Prefer **Webura → execute SQL** for one-off DDL/DML on your linked Supabase project.
- Keep reusable snippets here (e.g. `profiles.sql`, `rls_policies.sql`) as **documentation** for reviewers; apply them via the SQL tool so the remote database stays the source of truth.
